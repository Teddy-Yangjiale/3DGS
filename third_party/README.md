# Third Party Code

Clone external method repositories here on the server or local machine.

```bash
git clone --recursive https://github.com/lkeab/gaussian-grouping third_party/gaussian-grouping
```

The actual third-party source tree is not committed to this repository. Record the upstream commit hash in `docs/reproduction.md` when running experiments.

